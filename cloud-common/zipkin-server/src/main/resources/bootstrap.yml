spring:
  application:
    name: zipkin

  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: jack
        password: jack

encrypt:
  failOnError: false

